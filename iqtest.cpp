#include <bits/stdc++.h>
using namespace std;

int main() {
    vector<vector<char>> v(4, vector<char>(4, ' '));
    for (long i = 0; i < 4; ++i) {
        for (long j = 0; j < 4; ++j) {
            cin >> v[i][j];
        }
    }
    
}